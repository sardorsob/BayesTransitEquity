# CHANGELOG.md
Major milestones and version markers.

---

## [Unreleased]

- **2026-04-17** — **nb07 (`07_intervention_simulation.ipynb`) run to completion — pipeline closed; Fig 1 + Fig 9 produced; dashboard GeoJSONs exported.** All 10 sections executed for primary estimand `fit_raw_zscore_x`; 9 tables + 2 figures + 3 GeoJSONs + 1 manifest archived. Headline results (**F031**):
  - **Bayesian targeting (top-20 by exceedance) vs Deterministic targeting (bottom-20 by det_jobs):** 6/20 target overlap; 12 threshold crossings vs 8 (P > 0.5 → < 0.5 at ≥ 80% posterior certainty) — **Bayesian ≈ 50% more efficient per intervened tract**; 1.99% vs 1.08% population-weighted reach.
  - **Spillover-dominant effect:** **11 of the 12 crossings under Scenario A are indirect** — queen-contiguous, boundary-ambiguous neighbours lifted by the 0.1 × Δ spillover weight. The 20 directly-targeted tracts start at posterior P ≈ 1.0 and a 50% gap-closure moves them to ≈ 0.98, not below 0.5. The policy signal is in the ambiguous neighbours, not the extreme-desert targets.
  - **Hook tract (06073013317, not targeted, coin-flip at baseline):** baseline exceedance 0.500 → **0.027** under Scenario A; Δ = **−0.472**, 95% CI [−0.478, −0.467], `prob_improve = 1.00`. A pure spillover flip from coin flip to confident adequacy — the paper's opening paradigm case, now vindicated quantitatively.
  - **Det-vs-Bayes classification across all 720 tracts:** **178 TP, 2 FP, 0 FN, 540 TN.** Zero false negatives means det and Bayes *agree on WHO is a desert*; the 14-of-20 divergence in the top-20 priority lists is a **ranking-within-the-desert-set** story, not a classification story. Reframes paper claim #4 from "Bayesian reveals hidden deserts" to **"Bayesian re-orders priority among the agreed-deserts AND exposes boundary-ambiguous intervention-responsive tracts"**.
  - **Calibration (nb07 §3):** Spearman(AM-peak min headway, det_jobs) = **−0.53** across 720 tracts; all 40 target tracts have ≥ 1 MTS route within 400 m. Parametric Δ on log1p(jobs) scale calibrated at strength=0.5 (median Δ = 1.54 for Bayes targets, 1.93 for det targets; ≈ +2,525 / +1,871 jobs respectively). Sensitivity grid over strength ∈ {0.25, 0.5, 0.75} is monotone and does not flip A-vs-B ordering.
  - **Equity Spearman barely moves** (Δρ_A = +0.0034, Δρ_B = +0.0069): intervening on 2.8% of tracts cannot shift the county-wide gradient. Paper should cite the crossings + hook results, not Δρ.
  - **Paper figures complete:** Fig 1 `pipeline__07_study_area_map.png` (SD tracts + 4,373 MTS + 1,823 NCTD stops + hook tract). Fig 9 `pipeline__07_intervention_fig9__fit_raw_zscore_x.png` (3-panel: baseline exceedance / Δ map with 12 stars + 14 det-only circles / efficiency bar chart).
  - **Dashboard (D002/D007 INTERFACES.md contract):** `data/processed/geojson/sd_tracts_equity_baseline.geojson` (720 features) + `scenarios/freq_double_{bayesian,det}_top20.geojson` + `dashboard_manifest.json` ready to ship to `app/`.
  - **CI-column NaN note:** the summary table's `ci_lower` / `ci_upper` columns are empty on every row except the hook — **by design.** Per-tract 95% bootstrap CIs (400 resamples over 32,000 draws) for all 720 tracts live in `pipeline__07_intervention_posterior__fit_raw_zscore_x.csv` (zero NaNs). Most summary-row metrics are scalar point estimates (counts, Spearman, percentages) that do not carry a bootstrap CI in this framework.
  - **STATUS.md / PAPER.md / structure.md / RISKS.md / FINDINGS.md** all updated. Phase 3 (analysis) complete; project now in **Phase 4 — paper writing + dashboard wiring**.

- **2026-04-17** — **nb06 (`06_equity_decomposition.ipynb`) run to completion — equity decomposition + model validation.** All 11 sections executed for primary estimand `fit_raw_zscore_x`; 15 tables + 9 figures archived under `pipeline__06_*__fit_raw_zscore_x.*`. Headline results:
  - **F028 — multi-destination equity:** Spearman(posterior-mean vs disadvantage_z) is pro-poor at every threshold × every destination: Jobs +0.47, Hospitals +0.36, **Groceries +0.58** (strongest), Schools +0.52 at 45 min. Composite-deficit set (> 50% desert probability on all four destinations) = **120 tracts**; **80%** of those have `disadvantage_z < 0` — intersectional accessibility deserts concentrate in affluent suburbs. Most-disadvantaged quartile has **2.8×** more jobs on average than least-disadvantaged and a **10×** lower desert-exceedance rate.
  - **F030 — robustness:** equity Spearman = **0.4699** to 4 d.p. across 9 prior configs (`beta_sigma ∈ {0.2, 0.3, 0.5} × nu ∈ {3, 4, 6}`) and 4 drop-one covariate specs. β[disadvantage_z] moves 0.10 → 0.27 when `log_pop_density` is dropped (F014 density-confound signature).
  - **F029 — diagnostic concerns (honest limitations):** PSIS-LOO Pareto k catastrophic (100% of tracts > 0.7; 82% > 1.0; elpd_loo uninterpretable — known pathology for per-tract spatial random effects); PPC Bayesian p-values ∈ {0, 1} on mean/sd/min/max (fixed `obs_sigma = 0.05` artefact); raw-X residual Moran's I = **0.6432** (p=0.001) — *higher* than Spatial+ (0.5724), contradicting D011's prediction.
  - **DECISIONS.md:** **D013** supersedes D012 (nb06 repurposed from deprecated "divergence metrics" to active equity-decomposition slot). **STATUS.md / PAPER.md / structure.md** updated with nb06 artefact index + paper claims. **nb07 (intervention simulation) remains the next pipeline step**; F029 does not block it because the posterior draws themselves are converged (F025).

- **2026-04-08** — **nb04:** `k_remove` default **5%** for Spatial+; env **`PIPELINE_NO_SPATIAL_PLUS`** for raw-**X** primary path (**D011**); **`draws` ≥ 10k** when Spatial+ on; Moran residual diagnostic → **`pipeline__04_moran_residual_summary__<RID>.csv`**. **nb05:** KDE fan chart (jobs), **Wasserstein** map+CSV, multi-threshold Spearman, hook candidates; **PAPER.md** Fig 8 source → nb05. **FINDINGS F026**; **DECISIONS D011**. Patch script: `scripts/patch_nb04_spatial_moran.py` (already applied to ipynb).

- **2026-04-07** — **Context: nb04 dual-run results (F024, F025).** `FINDINGS.md` documents side-by-side **2026-04-05** (z-scored **X**) vs **2026-04-06** (**Spatial+** **X**): stable Spearman equity diagnostics, shifting **β** / **σ**; production MCMC (**fixed** `obs_noise`, 8k/4k, `target_accept` 0.99) supersedes **F022** variance failure. `PAPER.md`, `STATUS.md`, and cross-reference rows in **FINDINGS** updated; **F021** flagged as partial historical seven-predictor run.

- **2026-04-03** — **nb04 likelihood / MCMC defaults:** `build_tract_bym_normal` gains `likelihood` (`normal` | `student_t`), `obs_noise` (`fixed` | `estimated`), `fixed_obs_sigma`, `estimated_obs_sigma_prior_scale`, `student_t_nu`. Default **`obs_noise: fixed`** removes the sampled `sigma_obs` RV to ease σ vs observation-noise identifiability. `configs/defaults.yaml`: **`tune: 2000`**, **`target_accept: 0.95`**, new model keys wired in `notebooks/04_bayesian_model.ipynb`; diagnostics use `_diag_vars` so trace/summary omit `sigma_obs` when fixed.

- **2026-04-03** — **nb04 re-run reviewed; context updated (STATUS, FINDINGS, PAPER).** Diagnostics CSV `pipeline__04_model_diagnostics__2026-04-03.csv`: `rho` and fixed effects mix well (R-hat ~1.00, ESS ≫ 400 for betas); `sigma` / `sigma_obs` still high R-hat (~1.07 / ~1.32) and low ESS (~38 / ~10); 189 divergences / 8k draws. Equity Spearman: posterior mean jobs vs disadvantage_z ρ ≈ +0.52; posterior SD vs disadvantage ρ ≈ −0.07, *p* ≈ 0.08 (nonsignificant). See **F021–F023** (revised).

- **2026-04-03** — **nb04 + src/modeling full overhaul — ready to re-run.**
  - `src/modeling/spatial.py` rewritten to disk (was 0 bytes on disk; git version had id_order bug). Fixed `_id_order_to_geoids_and_geoms()`: when libpysal returns integer row-positions as id_order, use `iloc[pos]` not `str(pos).zfill(11)` to get real GEOIDs.
  - `src/modeling/tract_bym.py` completely overhauled: new function signature `build_tract_bym_normal(*, W, scaling_factor, X, y_std, geoids, cov_names)`. Accepts pre-standardised y (mean=0, sd=1). New priors: `rho ~ Beta(2,2)` (was Beta(0.5,0.5)), `sigma ~ Exponential(lam=2)` (PC prior, was HalfNormal(1.0)), `sigma_obs ~ HalfNormal(0.2)` (tight, was HalfNormal(0.5)). Added `posterior_summary()` helper for back-transform.
  - `notebooks/04_bayesian_model.ipynb` rebuilt end-to-end (16 cells). Now includes: explicit y standardisation with stored (y_mean, y_sd), prior predictive check, `tune=2000`, `target_accept=0.95`, `CHAINS=4`, back-transform of posteriors, exceedance probability, p_transit_desert, equity Spearman, 3-panel posterior maps, posterior predictive check, model meta JSON export for nb05.

- **2026-04-03** — **nb04 BYM2 model first run — partial results, convergence failure on variance parameters.** Beta coefficients recovered (log_pop_density +0.683, log_median_income −0.174 are the only credible non-zero effects; race/poverty cross zero — F021). Spatial mixing ρ ≈ 0.76. Equity Spearman: posterior mean jobs ρ = +0.54 vs disadvantage_z (density confound confirmed; F023). CRITICAL: rho/sigma/sigma_obs R-hat > 1.10, ESS_bulk < 30, 438 divergences (5.5%) — variance parameters unconverged; CIs and exceedance probs unreliable (F022). Root cause: Beta(0.5,0.5) prior on rho trapping chains near boundary + sigma/sigma_obs non-identifiability. Fix required before nb05: tighten priors, increase tune to 2000, target_accept to 0.95.

- **2026-04-03** — **Pipeline nb03 complete (accessibility).** Full r5py run: chunked tract–tract OD, LODES cumulative jobs at 30/45/60 min, OSM POI OD + tract rollups, merged **`tract_accessibility_bundle__2026-04-03.parquet`**. Summary + choropleth artifacts; Spearman **jobs vs disadvantage_z ≈ +0.47** at 45 min (**F020**). `context/STATUS.md`, `structure.md`, `FINDINGS.md`, `PAPER.md` updated.

- **2026-04-01** — **Pipeline unblocked — R5 NPE root causes fixed.** Three independent bugs each caused `java.lang.NullPointerException` in `TransitLayer.loadFromGtfs()` during nb03:
  1. **`parent_station` orphan refs** — `prefix_feed()` in nb02 prefixed `stop_id` but not `parent_station`; 306 stops referenced raw MTS IDs (e.g. `encsta`, `utc`) that no longer existed after prefixing. Fix: nb02 now also prefixes `parent_station`. Processed GTFS zip patched to clear existing orphans.
  2. **NCTD routes missing `agency_id`** — NCTD's `routes.txt` omits the `agency_id` column (valid GTFS for single-agency feeds); after merge, 47 NCTD routes had `agency_id = NaN`. Fix: nb02 `prefix_feed()` now synthesises `agency_id` when column is absent. Processed GTFS zip patched: `nctd:NCTD` added to `agency.txt`, all 47 routes assigned correct `agency_id`.
  3. **`configs/san_diego.yaml` truncated** — file was only 960 bytes, missing `r5:`, `census:`, and NCTD agency blocks. Restored from git (2,231 bytes).
  - Additional fixes: osmium `.EXE` uppercase suffix on Windows (`download_data.py`), TIGER FIPS string comparison in nb03 cell 2, Java setup merged into nb03 r5py import cell, `src/utils/paths.py` restored